** Fix in Lexer:
        Add error messages when include more than one char in \'

** Fix Parser:
        Express "this should be between spaces"
        Write better error messages (and enable parsing to continue)
        Activate cpp for Parser.y and rename it to Parser.y.pp
        Add the grammar rule to write `<string-literal>[<int>]>'

** Look in compiler/main/SysTools.lhs (ghc) for as, ld, llc, opt

** Fix in TcMonad:
        Report `not in scope' only once
        Add error in redefinitions
        Change getFuncNameM / getVarNameM

** Fix in TypeCheck:
        Add function name in tcRetStmtErr (with getNameM)
